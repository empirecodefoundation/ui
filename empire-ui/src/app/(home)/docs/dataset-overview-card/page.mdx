import { DatasetOverviewCardExample } from "@/components/examples/DatasetOverviewCardExample";
import ComponentCodePreview from "@/components/component-code-preview";
import ComponentInstall from "@/components/component-install";
import { Breadcrumbs } from "@/components/breadcrumbs";

<Breadcrumbs title={"Card"} />

# Dataset Overview Card

---

The **Dataset Overview Card** is an essential component designed for presenting an overview of datasets used in training models. It visually summarizes key information about the dataset, such as its size, number of features, and sample data, making it easier for users to grasp important metrics at a glance.

<ComponentCodePreview
  component={<DatasetOverviewCardExample />}
  filePath="examples/DatasetOverviewCardExample"
/>

## Installation

---

<ComponentInstall
  dependencies={"lucide-react framer-motion recharts"}
  componentFilePath={"core/dataset-overview-card"}
  cli={"npx empire@latest add dataset-overview-card"}
/>
